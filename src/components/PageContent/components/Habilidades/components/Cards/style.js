import styled, { keyframes } from "styled-components";

export const Wrapper = styled.div`
    height: 150px;
    color: white;
    display: flex;
    gap: 50px;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
`